# CLAUDE.md — table-to-chart

Canonical entry for agents on this repo is **[`AGENTS.md`](AGENTS.md)** (same folder). This file is a Claude Code cross-tool stub.

Reminders that bite:
- **Marker syntax is stable** — Chris's existing tables in personal-projects use it; don't break the format.
- **Auto-committed SVGs must use `[skip ci]`** — otherwise the action loops on itself.
